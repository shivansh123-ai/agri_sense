# Gemini API Key (Required for live AI features, falls back to high-fidelity mock data if empty)
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (Optional - defaults to local SQLite if empty)
SUPABASE_URL=
SUPABASE_KEY=
DATABASE_URL=

# Weather API Key (Optional - defaults to mock weather logic if empty)
OPENWEATHER_API_KEY=
